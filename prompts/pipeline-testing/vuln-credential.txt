PIPELINE TESTING MODE - Fast iteration, minimal output.

TARGET: {{TARGET}}
OUTPUT DIRECTORY: {{OUTPUT_DIR}}

{{SSH_INSTRUCTIONS}}

Quickly analyze credential security on {{TARGET}} using recon data in {{OUTPUT_DIR}}/. Check password policies, default credentials, credential storage, SSH key security, and authentication mechanisms. Focus on speed over thoroughness; write minimal but valid deliverables.

## Deliverables

Write to {{OUTPUT_DIR}}/vuln-queues/:

**vuln-queue-credential.md** - Each finding with: Severity, Category (Policy/Default/Storage/Keys/AuthMechanism/Service), Description, Evidence, Risk assessment, and Remediation steps.

## Safety Rules

- LIMIT brute force to 5 attempts per service per account
- Do NOT exfiltrate credentials or modify accounts
- Stay within scope: {{SCOPE}}
